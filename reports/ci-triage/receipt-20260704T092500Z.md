# CI Triage Receipt — 2026-07-04 09:25 UTC

**Triage pass:** hourly CI/build health watch #8
**Operator:** autonomous CI triage (no code changes made)
**Newest commit on branch (local):** `6e85f44c` (evidence(runpod): A6 live gpu_healthcheck PASSED — RTX 4090 visible)
**Newest commit on branch (remote/PR #3 head):** `677c77ed` (4 local commits unpushed)
**Prior receipt:** `reports/ci-triage/receipt-20260704T091200Z.md` (09:12 UTC)
**Delta window:** 09:12 UTC → 09:25 UTC (2026-07-04) (~13 min)
**Time source:** GitHub Actions run timestamps (canonical). Local system clock is skewed ~−4h; GitHub timestamps used throughout.

---

## Checked workflows (7 active)

| Workflow | Latest run | Conclusion | Delta since last receipt |
|----------|-----------|------------|--------------------------|
| `ci` | 28686245962 (`677c77ed`) | failure | UNCHANGED — no new run since 22:35 UTC (2026-07-03). Same Ruff lint failure, 1334 errors. |
| `build-runpod-training` | 28686244617 (`677c77ed`) | success | UNCHANGED — GREEN (no new run since 22:35 UTC) |
| `build-images` | 28645062751 (`c2d895b9`) | failure | UNCHANGED — no new run since 07:19 UTC (2026-07-03) |
| `build-runpod-smoke` | 28647224182 (`87fd0753`) | success | UNCHANGED — GREEN |
| `build-runpod-cuda-test` | 28655168752 (`349c194d`) | success | UNCHANGED — GREEN |
| `nightly` | **28701763911** (`c8219564` main) | failure | **NEW RUN** at 09:20:39 UTC (2026-07-04). Same SHA `c8219564` as prior nightly 28652985226 — scheduled re-run, not a code change. Same F3/F4 failures; Long tests now SUCCESS. |
| `aws-iac-validate` | 28315462854 (`598b2a81` main) | success | UNCHANGED — GREEN (last run 2026-06-28) |

No runs in `in_progress` / `queued` / `waiting` state at triage time.

One new run entered the delta window: the scheduled `nightly` on `main` (28701763911) at
09:20 UTC — the run that receipt #7 flagged as "expected ~10:00 UTC". It ran ~40 min early
but on the **same commit SHA** (`c8219564`) as the previous nightly, so its failures are a
scheduled re-execution of the same code, not a regression and not an escalation.

The active branch `fix/test-harness-optional-deps-guards` has **not been pushed** since
`677c77ed` at 22:35 UTC (2026-07-03). The 4 unpushed local evidence/receipt commits
(`748eef6c`, `3940271b`, `3098f11f`, `6e85f44c`) are unchanged in set and are doc/evidence-only.

---

## Failures found

### F1 — `ci` / Python lint + typecheck (Ruff) — UNCHANGED

| Field | Value |
|-------|-------|
| Workflow | `ci` |
| Run id | 28686245962 |
| Branch | `fix/test-harness-optional-deps-guards` |
| Commit SHA | `677c77ed1968408bcc0dec075d68c80087b71ca0` |
| Failed job | `Python lint + typecheck` |
| Failed step | `Ruff lint` (`uv run ruff check libs services`) |
| Classification | **test/type/lint failure** (pre-existing lint debt) |
| Error summary | `Found 1334 errors. [*] 613 fixable with --fix (171 hidden fixes via --unsafe-fixes).` Exit code 1. Identical error count across receipts #2–#8. Concentrated in `services/quant_foundry/` (S112, I001, B905, UP037, RUF046, B017, B904, SIM108, etc.). All other 6 ci jobs PASSED (Lockfile sync, Python tests + coverage, Startup safety matrix, Secret scan, JS lint + typecheck + test, Verification receipt). |
| Suspected owner/subsystem | `services/quant_foundry` (quant-ML / RunPod training code) |
| New since last receipt? | **No** — no new `ci` run triggered. Same failure as receipt #7. Also fails on `main`. |
| Blocks active RunPod fix? | **No.** `build-runpod-training` is green on `677c77ed`. |
| Exact next fix | `uv run ruff check --fix libs services` on a **separate branch** (auto-fixes 613/1334), then triage remaining 721. Do NOT mix with the RunPod fix. |

### F2 — `build-images` / build (api) — UNCHANGED (pending local fix observed)

| Field | Value |
|-------|-------|
| Workflow | `build-images` |
| Run id | 28645062751 |
| Branch | `fix/test-harness-optional-deps-guards` |
| Commit SHA | `c2d895b92d4ecf81e1a92039cfc1f7059c28939f` |
| Failed job | `build (api)` |
| Failed step | Docker buildx `RUN uv sync --frozen --no-dev --package api` (api.Dockerfile:36) |
| Classification | **Docker/build failure** (pre-existing) |
| Error summary | `error: Failed to determine installation plan` / `Distribution not found at: file:///build/experiments/news-impact-model`. `uv.lock` pins workspace member `experiments/news-impact-model` as a path dep; builder stage lacks the `experiments/` dir. Other 6 image jobs PASSED. |
| Suspected owner/subsystem | `services/api` build / workspace `uv.lock` (news-impact-model experiment) |
| New since last receipt? | **No** — no new `build-images` run since 07:19 UTC (2026-07-03). |
| Blocks active RunPod fix? | **No.** RunPod image built by separate `build-runpod-training` workflow (green). |
| Pending local fix | **Uncommitted** in working tree: `infra/docker/api.Dockerfile` adds `COPY experiments experiments` after the `COPY services services` line. Verified (receipt #5): `experiments/news-impact-model/` exists and is referenced 6× in `uv.lock`. This one-line addition is the correct, minimal fix. **Not committed** — pre-existing local work, mixed with other uncommitted changes, and rule 7 requires keeping the RunPod fix path isolated. |
| Exact next fix | Stage only the Dockerfile: `git add infra/docker/api.Dockerfile && git commit -m "fix(docker): copy experiments dir into api builder for uv sync --frozen"` on a **separate branch** from the RunPod fix, then push to trigger `build-images`. |

### F3 — `nightly` / Trivy filesystem scan — NEW RUN, SAME FAILURE (no escalation)

| Field | Value |
|-------|-------|
| Workflow | `nightly` |
| Run id | **28701763911** (NEW; supersedes 28652985226) |
| Branch | `main` |
| Commit SHA | `c8219564d56f5691ebbfd9e2e215d0ae296f408b` (UNCHANGED from prior nightly) |
| Failed job | `Trivy filesystem scan` |
| Failed step | `aquasecurity/trivy-action@cf5c088a69634cd13ccddc735d7926162c31f9a6` (fs scan, severity CRITICAL,HIGH, exit-code 1) |
| Classification | **deployment/image publish failure**-adjacent (security scan gate) |
| Error summary | `Total: 5 (HIGH: 5, CRITICAL: 0)` in `next` 14.2.35 — CVE-2026-44573 (info disclosure via middleware), CVE-2026-44578 (SSRF via crafted request), plus 3 GHSA DoS advisories. Fixed in `next` 15.5.16 / 16.2.5. Plus `Total: 1 (HIGH: 0, CRITICAL: 1)`: **Stripe secret-token** (stripe-secret-token / Stripe Secret Key) leaked in repo. Identical finding set to the prior nightly — no new CVE, no new secret. |
| Suspected owner/subsystem | `apps/dashboard` (Next.js 14.2.35) + leaked Stripe secret |
| New since last receipt? | **Run is new** (09:20 UTC, scheduled). **Failure content is unchanged** — same SHA, same 5 HIGH + 1 CRITICAL, same CVEs and same Stripe secret. Not an escalation. |
| Blocks active RunPod fix? | **No.** Nightly on `main`; RunPod image built by `build-runpod-training` (green). |
| Exact next fix | **(urgent, security)** `trivy fs --scanners secret --severity CRITICAL .` → locate & remove Stripe token → **rotate the Stripe key**. Then bump `next` to >=15.5.16 in `apps/dashboard/package.json`. Do on `main` or a dedicated security branch, NOT the RunPod fix branch. |

### F4 — `nightly` / Python dependency audit — NEW RUN, SAME FAILURE (no escalation)

| Field | Value |
|-------|-------|
| Workflow | `nightly` |
| Run id | **28701763911** (same run as F3) |
| Branch | `main` |
| Commit SHA | `c8219564d56f5691ebbfd9e2e215d0ae296f408b` |
| Failed job | `Python dependency audit` |
| Failed step | `uv run pip-audit --ignore-vuln GHSA-4xh5-x5gv-qwph` |
| Classification | **dependency/install failure** (audit gate) |
| Error summary | `Found 53 known vulnerabilities, ignored 1 in 15 packages`. Confirmed hit list (unchanged): `certifi 2023.11.17` (PYSEC-2024-230), `configobj 5.0.8` (CVE-2023-26112), `cryptography 41.0.7` (PYSEC-2024-225, PYSEC-2026-35, CVE-2023-50782, CVE-2024-0727, GHSA-h4gh-qq45-vh27, CVE-2026-26007, GHSA-537c-gmf6-5ccf), `idna 3.6` (PYSEC-2024-60, PYSEC-2026-215), `jinja2 3.1.2` (CVE-2024-22195, CVE-2024-34064, CVE-2024-56326, CVE-2024-56201, CVE-2025-27516), `pip 24.0` (PYSEC-2026-196, CVE-2026-1703, CVE-2026-3219, CVE-2026-6357), `pyasn1 0.4.8` (CVE-2026-30922), `pygments 2.17.2` (CVE-2026-4539), `pyjwt 2.7.0` (PYSEC-2026-120, PYSEC-2025-183, PYSEC-2026-179, PYSEC-2026-175, PYSEC-2026-177), `pyopenssl 23.2.0` (CVE-2026-27448, CVE-2026-27459), `requests 2.31.0` (CVE-2024-35195, CVE-2024-47081, CVE-2026-25645), + others. Same 53/15 as prior nightly. |
| Suspected owner/subsystem | Python workspace transitive deps (`uv.lock`) |
| New since last receipt? | **Run is new** (09:20 UTC). **Failure content unchanged** — same SHA, same 53 vulns / 15 packages. Not an escalation. |
| Blocks active RunPod fix? | **No.** |
| Exact next fix | `uv lock --upgrade` (refresh certifi, cryptography, idna, jinja2, pip, pyjwt, pyopenssl, requests + the rest), re-run `uv run pip-audit --ignore-vuln GHSA-4xh5-x5gv-qwph`. On a **separate branch** from the RunPod fix. |

### Note on `nightly` Long tests

The `Long tests` job in run 28701763911 **PASSED** (completed 09:21 UTC). This is the
only job in the new nightly run whose result differs in posture from the prior nightly
(prior receipt did not single it out, but it is green now). No action required.

---

## Failures ignored as transient

None. The only new run in the delta window (nightly 28701763911) is a scheduled re-run on
an unchanged SHA; its failures are real (F3/F4) but unchanged, not transient. No runner
infrastructure errors, no cancellations, no timeouts observed.

---

## Changes made

**None.** No broad fixes attempted per triage policy. The one new run (nightly 28701763911)
is on the same SHA as the prior nightly, so its failures are pre-existing debt (F3/F4),
not a regression introduced in this delta window. Rule 6 (tiny, obvious, isolated fix)
does not apply — F3 requires a Stripe key rotation (out of scope for an autonomous triage
pass) and F4 requires a `uv lock --upgrade` on a separate branch.

The uncommitted one-line fix for F2 remains present in the working tree
(`infra/docker/api.Dockerfile`: `COPY experiments experiments`) — correct and minimal,
but not committed by this triage pass (pre-existing local work, mixed with other uncommitted
changes, rule 7 isolation). See F2 above for the exact staging command.

The active RunPod training-worker fix (`build-runpod-training` workflow) is
**green on `677c77ed`** (run 28686244617). No workflow failure blocks it. Four
local evidence/receipt commits (`748eef6c`, `3940271b`, `3098f11f`, `6e85f44c`)
remain unpushed but are doc/evidence-only and do not affect CI.

---

## Exact next action

No CI blocker. No new failures (the new nightly run is a same-SHA re-run). Two tracks:

1. **RunPod fix (active):** continue per `docs/runpod-fix-plan/RECEIPT_INDEX.md`.
   `build-runpod-training` is green. The 4 unpushed commits are doc/evidence-only
   — push when ready; they will not trigger `ci` (push event) but will re-trigger
   `build-runpod-training` (expected green).

2. **CI debt cleanup (separate, when bandwidth allows):**
   - **F3 (security, urgent):** locate & remove Stripe secret, **rotate the key**,
     bump `next` >=15.5.16 in `apps/dashboard/package.json`. The CRITICAL secret
     finding has now persisted across 2 nightly runs — do not let it sit.
   - **F2 (quick win):** commit the pending `api.Dockerfile` one-liner on a
     separate branch → push → verify `build-images` build(api) goes green.
     `git add infra/docker/api.Dockerfile && git commit -m "fix(docker): copy experiments dir into api builder for uv sync --frozen"`
   - **F1 / F4:** `ruff --fix` pass and `uv lock --upgrade` on separate branches.

**Next watch:** re-run this triage in ~1 hour, or immediately after the next
push to `fix/test-harness-optional-deps-guards` or `main`. The next scheduled
`nightly` is expected ~09:20 UTC tomorrow (2026-07-05); F3/F4 will keep failing
on `main` until the Stripe secret is removed and `next` is bumped.
