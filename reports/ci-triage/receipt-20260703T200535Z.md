# CI Triage Receipt — 2026-07-03 20:05 UTC

**Triage pass:** first CI-triage receipt on branch `fix/test-harness-optional-deps-guards`
**Operator:** autonomous CI triage (no code changes made)
**Newest commit on branch:** `c0f15fa7` (test(runpod): Test F import bisection handler)
**Baseline note:** No prior CI-triage receipt existed. The RunPod fix-plan
`RECEIPT_INDEX.md` tracks the training-worker investigation, not CI failures.
Pre-existing failures were verified against `main` (SHA `c8219564`, 2026-06-28)
to distinguish regression from debt.

---

## Checked workflows (7 active)

| Workflow | Latest run | Conclusion | Notes |
|----------|-----------|------------|-------|
| `ci` | 28679953580 (`c0f15fa7`) | failure | Ruff lint — pre-existing debt |
| `build-runpod-training` | 28679951739 (`c0f15fa7`) | success | GREEN — active RunPod fix path healthy |
| `build-images` | 28645062751 (`c2d895b9`) | failure | `build (api)` — pre-existing debt |
| `build-runpod-smoke` | 28647224182 (`87fd0753`) | success | GREEN |
| `build-runpod-cuda-test` | 28655168752 (`349c194d`) | success | GREEN (prior failure `883ddb50` resolved) |
| `nightly` | 28652985226 (`c8219564` main) | failure | Trivy + pip-audit — pre-existing |
| `aws-iac-validate` | 28315462854 (`598b2a81` main) | success | GREEN (last run 2026-06-28) |

No runs in `in_progress` / `queued` / `waiting` state at triage time.

---

## Failures found

### F1 — `ci` / Python lint + typecheck (Ruff)

| Field | Value |
|-------|-------|
| Workflow | `ci` |
| Run id | 28679953580 |
| Branch | `fix/test-harness-optional-deps-guards` |
| Commit SHA | `c0f15fa7be38460c6c1930ef5394caf152615199` |
| Failed job | `Python lint + typecheck` (job 85061245640) |
| Failed step | `Ruff lint` (`uv run ruff check libs services`) |
| Classification | **test/type/lint failure** (pre-existing lint debt) |
| Error summary | `Found 1334 errors. [*] 613 fixable with --fix (171 hidden fixes via --unsafe-fixes).` Exit code 1. Errors concentrated in `services/quant_foundry/` (calibration.py, catboost_trainer.py, artifact_io.py, cli/runpod_cli.py, tests/test_xgboost_trainer.py, etc.). Rule families: S112, I001, B905, UP037, RUF046, S301, F841, B017, UP035. |
| Suspected owner/subsystem | `services/quant_foundry` (quant-ML / RunPod training code) |
| New since last receipt? | No — identical 1334-error count on prior run 28679488624 (`c990476a`); same job failing on `main` since 2026-06-28 (`c8219564`). Pre-existing debt, not a regression from this branch. |
| Blocks active RunPod fix? | **No.** The `ci` workflow is not the RunPod build path. `build-runpod-training` is green. |
| Exact next fix | `uv run ruff check --fix libs services` (auto-fixes 613 of 1334), then `uv run ruff check libs services` to triage the remaining 721. Do NOT commit the auto-fix alongside the RunPod fix — keep it on a separate branch to avoid unrelated churn. |

### F2 — `build-images` / build (api)

| Field | Value |
|-------|-------|
| Workflow | `build-images` |
| Run id | 28645062751 |
| Branch | `fix/test-harness-optional-deps-guards` |
| Commit SHA | `c2d895b92d4ecf81e1a92039cfc1f7059c28939f` |
| Failed job | `build (api)` (job 84949641615) |
| Failed step | Docker buildx `RUN uv sync --frozen --no-dev --package api` (api.Dockerfile:36) |
| Classification | **Docker/build failure** (pre-existing) |
| Error summary | `error: Failed to determine installation plan` / `Caused by: Distribution not found at: file:///build/experiments/news-impact-model`. `uv.lock` pins workspace member `experiments/news-impact-model` (pyproject.toml:27,46) as a path dep; `uv sync --frozen` cannot resolve the local source distribution inside the builder stage. Other 6 image jobs (ingestor, agents, orchestrator, oms, risk, both runpod) PASSED. |
| Suspected owner/subsystem | `services/api` build / workspace `uv.lock` (news-impact-model experiment) |
| New since last receipt? | No — same `build (api)` failure on `main` since 2026-06-28 (`c8219564`, run 28320248346). Pre-existing. No `build-images` run has been triggered for the recent RunPod fix commits (last triggered at `c2d895b9`, 07:19 UTC). |
| Blocks active RunPod fix? | **No.** The RunPod training image is built by the separate `build-runpod-training` workflow, which is green. |
| Exact next fix | Investigate why `uv sync --frozen` cannot build the `news-impact-model` path dist in the builder stage. Likely options: (a) ensure `hatchling` build backend is available to uv in the builder image, (b) drop `news-impact-model` from the `api` package's transitive workspace resolution if the API doesn't import it at runtime, or (c) pre-build the wheel. Reproduce locally: `docker build -f infra/docker/api.Dockerfile -t fincept-api:debug .` |

### F3 — `nightly` / Trivy filesystem scan

| Field | Value |
|-------|-------|
| Workflow | `nightly` |
| Run id | 28652985226 |
| Branch | `main` |
| Commit SHA | `c8219564d56f5691ebbfd9e2e215d0ae296f408b` |
| Failed job | `Trivy filesystem scan` (job 84975249964) |
| Failed step | Trivy filesystem scan (exit code 1) |
| Classification | **deployment/image publish failure**-adjacent (security scan gate) |
| Error summary | `Total: 5 (HIGH: 5, CRITICAL: 0)` in `next` (Next.js 14.2.35): CVE-2026-44573 (info disclosure via middleware), CVE-2026-44578 (SSRF), GHSA-8h8q-6873-q5fj (DoS), GHSA-h25m-26qc-wcjf (DoS), GHSA-q4gf-8mx6-v5v3 (DoS). Plus `Total: 1 (CRITICAL: 1)`: **Stripe secret-token** leaked in repo. |
| Suspected owner/subsystem | `apps/dashboard` (Next.js 14.2.35) + leaked Stripe secret (location TBD — needs `trivy fs --scanners secret .` locally to pinpoint) |
| New since last receipt? | Recurring — nightly fails daily on `main` (2026-07-01, 07-02, 07-03 all on `c8219564`). The Stripe CRITICAL secret is the most actionable item. |
| Blocks active RunPod fix? | **No.** Nightly runs on `main`, independent of the RunPod branch. |
| Exact next fix | **(urgent, security)** Locate and remove the leaked Stripe secret token: `trivy fs --scanners secret --severity CRITICAL .` then rotate the Stripe key. **(separate)** Bump `next` from 14.2.35 to >=15.5.16 (or 16.2.5) in `apps/dashboard/package.json` to clear the 5 HIGH CVEs. |

### F4 — `nightly` / Python dependency audit

| Field | Value |
|-------|-------|
| Workflow | `nightly` |
| Run id | 28652985226 |
| Branch | `main` |
| Commit SHA | `c8219564d56f5691ebbfd9e2e215d0ae296f408b` |
| Failed job | `Python dependency audit` (job 84975250043) |
| Failed step | `uv run pip-audit --ignore-vuln GHSA-4xh5-x5gv-qwph` (exit code 1) |
| Classification | **dependency/install failure** (audit gate) |
| Error summary | `Found 53 known vulnerabilities, ignored 1 in 15 packages`. First hit: `certifi 2023.11.17` (PYSEC-2024-230, fix 2024.7.4). 15 packages affected. |
| Suspected owner/subsystem | workspace Python deps (`uv.lock`) |
| New since last receipt? | Recurring nightly failure on `main`. |
| Blocks active RunPod fix? | **No.** Nightly on `main`. |
| Exact next fix | `uv lock --upgrade` to refresh vulnerable packages (certifi >= 2024.7.4 and the other 14), then re-run `uv run pip-audit --ignore-vuln GHSA-4xh5-x5gv-qwph`. Triage remaining unfixable ones with `--ignore-vuln` only after confirming non-exploitability. |

---

## Failures ignored as transient

| Run | Job | Reason |
|-----|-----|--------|
| 28678984306 (`ci`, `233a733b`) | whole run `cancelled` | Superseded by newer push (`d7ba5a2d`) 9s later — cancellation, not a real failure. |
| 28668679718 (`ci`, `bf2ef399`) | whole run `cancelled` | Superseded by newer push 3s later — cancellation. |
| 28654830650 (`build-runpod-cuda-test`, `883ddb50`) | failure | Resolved by next commit `349c194d` (now green). Not current. |

---

## Changes made

**None.** No code changes were made. All four real failures are pre-existing
debt on `main` or unrelated to the active RunPod training-worker fix. Per the
triage policy, no broad fixes were attempted and no isolated one-line fix was
obvious enough to apply without risking unrelated churn on the active branch.

The active RunPod training-worker fix (`build-runpod-training` workflow) is
**green on every push** for the recent commits (`d7ba5a2d`, `c0f15fa7`). No
workflow failure is blocking it.

---

## Exact next action

Two independent tracks, neither blocks the RunPod fix:

1. **RunPod fix (continue current branch):** proceed with the plan in
   `docs/runpod-fix-plan/RECEIPT_INDEX.md` — fix the `parents[5]` IndexError in
   `equities.py:35` and `news.py:49`, restore the production handler as the
   direct entrypoint, rebuild via `build-runpod-training`, and retest live.
   No CI blocker stands in the way.

2. **CI debt cleanup (separate branch, do NOT mix with RunPod fix):**
   - **Security first:** run `trivy fs --scanners secret --severity CRITICAL .`
     to locate the leaked Stripe token, remove it, rotate the key. This is the
     only item with real-world urgency.
   - Then: `uv run ruff check --fix libs services` (clears 613/1334 lint errors).
   - Then: bump `next` to >=15.5.16 in `apps/dashboard/package.json`.
   - Then: `uv lock --upgrade` + re-run pip-audit for the 53 Python vulns.
   - Then: fix the `build (api)` `news-impact-model` path-dist resolution.
