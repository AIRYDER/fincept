# CI Triage Receipt — 2026-07-03 21:30 UTC

**Triage pass:** hourly CI/build health watch #2
**Operator:** autonomous CI triage (no code changes made)
**Newest commit on branch:** `6dbec436` (fix(runpod): parents[5] IndexError + restore production handler + probe fix)
**Prior receipt:** `reports/ci-triage/receipt-20260703T200535Z.md` (20:05 UTC)
**Delta window:** 20:05 UTC → 21:30 UTC (85 min)

---

## Checked workflows (7 active)

| Workflow | Latest run | Conclusion | Delta since last receipt |
|----------|-----------|------------|--------------------------|
| `ci` | 28683992505 (`6dbec436`) | failure | NEW run — same Ruff lint failure (1334 errors, identical) |
| `build-runpod-training` | 28683991294 (`6dbec436`) | success | NEW run — GREEN (active RunPod fix path healthy) |
| `build-images` | 28645062751 (`c2d895b9`) | failure | unchanged — no new run since 07:19 UTC |
| `build-runpod-smoke` | 28647224182 (`87fd0753`) | success | unchanged — GREEN |
| `build-runpod-cuda-test` | 28655168752 (`349c194d`) | success | unchanged — GREEN |
| `nightly` | 28652985226 (`c8219564` main) | failure | unchanged — no new run since 09:55 UTC |
| `aws-iac-validate` | 28315462854 (`598b2a81` main) | success | unchanged — GREEN (last run 2026-06-28) |

No runs in `in_progress` / `queued` / `waiting` state at triage time.

---

## Failures found

### F1 — `ci` / Python lint + typecheck (Ruff) — UNCHANGED

| Field | Value |
|-------|-------|
| Workflow | `ci` |
| Run id | 28683992505 |
| Branch | `fix/test-harness-optional-deps-guards` |
| Commit SHA | `6dbec436c92b57a788b84622338baacc3df8665d` |
| Failed job | `Python lint + typecheck` (job 85073040740) |
| Failed step | `Ruff lint` (`uv run ruff check libs services`) |
| Classification | **test/type/lint failure** (pre-existing lint debt) |
| Error summary | `Found 1334 errors. [*] 613 fixable with --fix (171 hidden fixes via --unsafe-fixes).` Exit code 1. Identical error count to prior receipt. Concentrated in `services/quant_foundry/`. Rule families: S112, I001, B905, UP037, RUF046, S301, F841, B017, UP035, B904, SIM108. |
| Suspected owner/subsystem | `services/quant_foundry` (quant-ML / RunPod training code) |
| New since last receipt? | **No** — same 1334-error count as run 28679953580 (`c0f15fa7`). Pre-existing debt, not a regression. |
| Blocks active RunPod fix? | **No.** `build-runpod-training` is green. |
| Exact next fix | `uv run ruff check --fix libs services` on a **separate branch** (auto-fixes 613/1334), then triage remaining 721. Do NOT mix with the RunPod fix. |

### F2 — `build-images` / build (api) — UNCHANGED

| Field | Value |
|-------|-------|
| Workflow | `build-images` |
| Run id | 28645062751 |
| Branch | `fix/test-harness-optional-deps-guards` |
| Commit SHA | `c2d895b92d4ecf81e1a92039cfc1f7059c28939f` |
| Failed job | `build (api)` (job 84949641615) |
| Failed step | Docker buildx `RUN uv sync --frozen --no-dev --package api` (api.Dockerfile:36) |
| Classification | **Docker/build failure** (pre-existing) |
| Error summary | `error: Failed to determine installation plan` / `Distribution not found at: file:///build/experiments/news-impact-model`. `uv.lock` pins workspace member `experiments/news-impact-model` as a path dep; `uv sync --frozen` cannot resolve it in the builder stage. Other 6 image jobs PASSED. |
| Suspected owner/subsystem | `services/api` build / workspace `uv.lock` (news-impact-model experiment) |
| New since last receipt? | **No** — no new `build-images` run triggered since 07:19 UTC. Same failure on `main` since 2026-06-28. |
| Blocks active RunPod fix? | **No.** RunPod image built by separate `build-runpod-training` workflow (green). |
| Exact next fix | Reproduce locally: `docker build -f infra/docker/api.Dockerfile -t fincept-api:debug .`. Likely: ensure hatchling build backend available in builder, or drop `news-impact-model` from `api` package's transitive workspace resolution. |

### F3 — `nightly` / Trivy filesystem scan — UNCHANGED

| Field | Value |
|-------|-------|
| Workflow | `nightly` |
| Run id | 28652985226 |
| Branch | `main` |
| Commit SHA | `c8219564d56f5691ebbfd9e2e215d0ae296f408b` |
| Failed job | `Trivy filesystem scan` (job 84975249964) |
| Classification | **deployment/image publish failure**-adjacent (security scan gate) |
| Error summary | `Total: 5 (HIGH: 5)` in `next` 14.2.35 (CVE-2026-44573, CVE-2026-44578, 3 GHSA DoS). Plus `Total: 1 (CRITICAL: 1)`: **Stripe secret-token** leaked in repo. |
| Suspected owner/subsystem | `apps/dashboard` (Next.js 14.2.35) + leaked Stripe secret |
| New since last receipt? | **No** — no new nightly run since 09:55 UTC. Recurring daily on `main`. |
| Blocks active RunPod fix? | **No.** Nightly on `main`. |
| Exact next fix | **(urgent, security)** `trivy fs --scanners secret --severity CRITICAL .` → locate & remove Stripe token → rotate key. Then bump `next` to >=15.5.16 in `apps/dashboard/package.json`. |

### F4 — `nightly` / Python dependency audit — UNCHANGED

| Field | Value |
|-------|-------|
| Workflow | `nightly` |
| Run id | 28652985226 |
| Failed job | `Python dependency audit` (job 84975250043) |
| Classification | **dependency/install failure** (audit gate) |
| Error summary | `Found 53 known vulnerabilities, ignored 1 in 15 packages`. First hit: `certifi 2023.11.17` (PYSEC-2024-230). |
| New since last receipt? | **No** — same run as F3. |
| Blocks active RunPod fix? | **No.** |
| Exact next fix | `uv lock --upgrade` (refresh certifi + 14 others), re-run `uv run pip-audit --ignore-vuln GHSA-4xh5-x5gv-qwph`. |

---

## Failures ignored as transient

None in this delta window. (Prior cancellations 28678984306 and 28668679718 already noted in previous receipt; no new cancellations.)

---

## Changes made

**None.** All four real failures are pre-existing debt identical to the prior
receipt — no new failures introduced in the 85-minute delta window. Per triage
policy, no broad fixes were attempted and no isolated one-line fix was obvious
enough to apply without risking unrelated churn on the active RunPod branch.

The active RunPod training-worker fix (`build-runpod-training` workflow) is
**green on the newest commit** `6dbec436` (run 28683991294, 21:24 UTC). No
workflow failure is blocking it.

---

## Exact next action

No CI blocker. Continue the RunPod fix per
`docs/runpod-fix-plan/RECEIPT_INDEX.md`. The four pre-existing CI debt items
(Ruff lint, build-images api, Trivy Stripe secret + Next.js CVEs, pip-audit)
remain on the separate cleanup track documented in the prior receipt — none
have escalated and none block the RunPod work.

**Next watch:** re-run this triage in ~1 hour, or immediately after the next
push to `fix/test-harness-optional-deps-guards`.
