# trainer-gpu-tabular-neural — Python package requirements (T-9.1)
#
# Source of truth: ImageSpec.packages in
#   src/quant_foundry/tabular_neural_runtime.py
#
# torch is provided by the base image (pytorch/pytorch:2.1.0-cuda12.1-cudnn8-runtime),
# so it is NOT reinstalled here to avoid pulling the CPU-only wheel and
# clobbering the CUDA build. The pins below match ImageSpec.packages.

numpy>=1.26
pandas>=2.1
scikit-learn>=1.3
pydantic>=2.7
